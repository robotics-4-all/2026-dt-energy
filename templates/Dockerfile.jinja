# Auto-generated by generate_twin.py for grid "{{ grid_name }}"
# Κοινό image για τα τρία custom Python services (edge_simulator, persistence_consumer,
# analytics_consumer) — το ποιο script τρέχει καθορίζεται από το `command:` στο
# docker-compose.yml, όχι εδώ, ώστε να μη χρειάζονται 3 ξεχωριστά Dockerfiles.
FROM python:3.11-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY edge_simulator.py persistence_consumer.py analytics_consumer.py ./

# Χωρίς ENTRYPOINT/CMD εδώ επίτηδες — ορίζεται per-service στο docker-compose.yml
